<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="caiococaro.com.br.energy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".ConsumoTempoReal" />
        <activity android:name=".HistoricoConsumo" />
        <activity android:name=".ReclamacaoEspecial" />
        <activity
            android:name=".AcompanhamEquipeTecnica"
            android:label="@string/title_activity_acompanham_equipe_tecnica"
            android:theme="@style/AppTheme" />
        <activity android:name=".CadastroClienteVital" />
        <activity android:name=".CadastroBaixaRenda"></activity>
    </application>

</manifest>